name: PHPUnit

on: [pull_request]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: PHPUnit
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.2'
        tools: phpunit


